

const initialState = {
    items: [],
    isRegistered: true,
}

export const appReduser = (state = initialState, action) => {
    switch (action.type) {
        // case :
        //     return {
        //         ...state,
                
        //     }
        // default:
        //     return state;
    }
    return state;
}